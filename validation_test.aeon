// Direct Quantum AST Validation Test
// Tests the code generation directly without file parsing

let number = 42;
let string_val = "hello";
let bool_val = true;

function simple_add(x, y) {
    return x + y;
}

let result = simple_add(10, 20);

// This will exercise our Binary expression handling
let arithmetic = number + 5;
let multiplication = number * 2;

// This will exercise assignment handling
string_val = "updated";

print("Validation complete");