fn main:
    call boot_threadsmiths
    let pattern = weave_pattern("Aurora", 5)
    log("Nebula thread matrix: " + pattern)
    call showcase_looms
    call report_status

fn report_status:
    log("All warp cores aligned.")