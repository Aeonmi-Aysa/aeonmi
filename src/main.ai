fn main:
    call banner
    print "Aeonmi shard booted"
    call render_pipeline
    call describe_logic
    let checksum = 21 + 21
    assert checksum == 42

fn verify_runtime:
    call verify_math
    call ensure_consistency

test runtime_health:
    call verify_runtime